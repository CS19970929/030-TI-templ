# SOC 第一阶段重构说明

## 本次目标

- 去掉首次启动固定 `60%` 的默认行为。
- 将 SOC 持久化改为快照式存储，兼容旧 EEPROM 格式回读。
- 将内部真实 SOC 与对外显示 SOC 分层，减少开机和末端跳变感。
- 尽量不动现有安时积分主路径，控制风险。

## 实现内容

### 1. 启动逻辑

启动恢复优先级改为：

1. 读取新格式双槽快照。
2. 若没有有效新快照，则兼容读取旧格式 SOC 存储。
3. 若存储也无效，则使用受限 OCV 初始化。
4. 若电压本身无效，再退化到低可信默认值 `50%`。

这样避免了原先“第一次上电直接 60%”的问题。

### 2. 持久化逻辑

原先是分散字段写入：

- 多个 `SOC` 槽位
- 放电累计量槽位
- 循环次数
- 满充容量
- 状态标志

现在改为同地址双槽快照：

- `magic`
- `seq`
- `soc`
- `dsg_soc_int`
- `cycle_times`
- `cap_full_ah`
- `status`
- `checksum`

启动时按 `magic + checksum` 校验并选择最新快照。

### 3. 显示分层

增加两层概念：

- `SOC_real`
  当前算法真实值，继续由现有 OCV/积分/端点修正规则驱动。
- `SOC_display`
  对外展示值，按秒级缓慢跟踪 `SOC_real`。

当前策略：

- 充电上升时允许更快追随。
- 小幅回落时尽量避免显示回跳。
- 初始化后显示值先与真实值对齐。

### 4. EEPROM 写入节流

不再每次检测到变化就立即写。

改为：

- 1 秒检查一次脏状态
- 持续脏 10 秒再落盘

这样可以显著减少 EEPROM 擦写频率。

## 兼容策略

- 升级后先尝试读取新格式快照。
- 若当前设备 EEPROM 里还是旧格式，会自动按旧布局回读一次。
- 一旦成功启动，会回写为新快照格式，完成平滑迁移。

## 当前仍保留的内容

- OCV 查表仍按现有表与现有最低单体逻辑处理。
- 充放电积分主流程保持不变。
- 末端 `CorrectionTerminal_CV()` 仍保留，避免一次性改太多。

## 下一阶段建议

第二阶段建议继续做：

- 静置判定与有限幅度 OCV 回正
- 零流偏置与静态功耗补偿
- 末端事件化校准，替代持续强拉
- `CapFull` 学习与 SOH 收敛
