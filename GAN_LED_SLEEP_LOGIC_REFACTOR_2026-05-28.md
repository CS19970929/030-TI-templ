# 干簧管、灯板、休眠逻辑重构说明

日期：2026-05-28

## 需求来源

依据 `V1.1电池逻辑(5).xlsx` 梳理实现，三只干簧管映射如下：

- `GAN1`：浮动连接器/插头在位，闭合为 ON。
- `GAN2`：把手在位，闭合为 ON。
- `GAN3`：电池上盖/灯板干簧管，闭合为 ON。

## 代码分层

本次把逻辑拆成三层：

- `gan_huang_guan_logi.c`：业务状态机，负责 GAN1/GAN2/GAN3、PA0 充电唤醒、充电锁存、进水保护和计时。
- `LedBar.c`：纯显示状态机，只执行 SOC 临时显示、开机动画、关机动画、充电动画和进水报警。
- `SleepDeal.c`：只处理低功耗入口和休眠唤醒判定，不再直接承担灯板按键业务。

`IO_Control.c` 中保护条件放在干簧管业务之后兜底覆盖，避免业务逻辑误打开故障/进水/CBC 等保护关断。

## 关键行为

- 电量临时查看：`GAN3` 闭合 1s 后显示 SOC，显示 3s 后关闭。
- 休眠态电量查看：`GAN3` 可唤醒到 SOC 预览；真正开机必须 `GAN1 && GAN2 && GAN3 持续 3s`。
- 放电开机：休眠态 `GAN1 && GAN2` 且 `GAN3` 持续 3s 后，复位进入正常模式，执行 LED1 到 LED5 逐一点亮、再从 LED5 到 LED1 熄灭的动画，之后 SOC 常亮。
- 放电关机：运行态 `GAN1 && GAN2` 且 `GAN3` 持续 3s 后，执行全亮后从 LED5 到 LED1 逐一熄灭动画，动画完成后进入深度休眠。
- 浮动连接器断开：非充电状态下 `GAN1` 断开 0.5s 后进入深度休眠，不跑关机动画。
- 把手提起：非充电状态下 `GAN2` 断开超过 2s 后进入深度休眠；2s 内恢复则继续放电。
- 充电：只要 `GAN1` 短接并检测到 PA0 插入或充电电流，锁存充电状态；充电状态不受 `GAN2` 和 `GAN3` 影响。
- 充电显示：检测到充电电流后按 0-20/20-40/40-60/60-80/80-99 分段闪烁，SOC >= 99 时 5 灯常亮。
- 充电断开：CHG MOS 打开后 PA0 会失效，因此使用“充电电流消失且 SOC 未满持续约 5s”判断断开；`GAN1` 断开立即进入休眠。
- 进水：立即关闭输出并全灯闪烁，2 分钟后休眠；`GAN1` 或 `GAN2` 断开时立即休眠。

## 时间参数

- `GAN3_SOC_TICKS_10MS = 100`：上盖闭合 1s 触发 SOC 临显。
- `GAN3_POWER_TICKS_10MS = 300`：上盖闭合 3s 触发开关机。
- `GAN1_OFF_SLEEP_TICKS_10MS = 50`：浮动连接器断开 0.5s 休眠。
- `GAN2_OFF_SLEEP_TICKS_10MS = 200`：把手提起 2s 休眠。
- `WATER_SLEEP_TICKS_10MS = 12000`：进水报警 2 分钟后休眠。
- `CHARGER_LOST_TICKS_10MS = 500`：充电电流消失约 5s 后判定断开。
- `LEDBAR_ANIM_STEP_TICKS_100MS = 2`：跑马灯每步约 0.2s。

## 充电判断约束

硬件上 CHG MOS 打开后 PA0 会变为低电平，因此 PA0 只能作为“充电器插入、打开 CHG MOS 前”的触发源，不能作为充电全过程在线信号。当前策略是：

1. `GAN1` 短接且 PA0 有效，或检测到充电电流，进入充电锁存。
2. 充电锁存后忽略 `GAN2` 和 `GAN3`。
3. `GAN1` 断开视为直流侧断开，立即休眠。
4. SOC 未满且充电电流持续消失约 5s，视为充电器断开。
5. SOC 已满时保持满电常亮，避免恒压末期低电流误判拔充电器。

## 构建验证

Keil 目标：`Target 1`

芯片：`STM32F030C8`

构建结果：

- `0 Error(s), 0 Warning(s)`
- Program Size：`Code=39072 RO-data=2668 RW-data=676 ZI-data=6356`
- 产物：
  - `Objects/CommomBQ769x0_16series_030C8T6_C.axf`
  - `Objects/CommomBQ769x0_16series_030C8T6_C.hex`
  - `Objects/CommomBQ769x0_16series_030C8T6_C.bin`

## 上板重点验证

- 休眠态上盖闭合 1s 只显示 SOC，未到 3s 松开后返回休眠。
- 休眠态 `GAN1 && GAN2` 下上盖闭合 3s 开机，开机动画后 SOC 常亮。
- 运行态上盖闭合 3s 关机，动画完成后进入休眠。
- 充电器插入后，即使 `GAN2` 断开或 `GAN3` 闭合，也不影响充电 MOS 持续打开。
- 充电中 `GAN1` 断开后立即休眠。
- 进水后全灯闪烁、输出关闭，2 分钟后休眠；提把手或断开浮动连接器立即休眠。
