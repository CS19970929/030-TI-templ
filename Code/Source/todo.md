
新增逻辑：休眠关机状态下，只要GPIO_GAN1短接，充电可唤醒进入正常状态，使用PA0上升沿唤醒，不需要开关长按,对了，所有的唤醒进入正常模式，都需要短接GPIO_GAN1，否则继续进入休眠,同时简化目前的IsSleepStartUp

可以在家里的mac上的codex上来控制、阅读、修改本机的各种工程，编译keil工程等吗？


P12 RS485
波特率: 115200
格式: 8N1
地址: 0x0301
校验: 逐字节累加和低16位，小端

静态查询:
21 AA 01 03 80 00 4F 01

动态查询:
21 AA 01 03 82 00 51 01

动态响应长度请按 Len 字段解析，不要写死。

static UINT32 P12_GetStatusFlags(void)
//todo 需要确认状态、定义



soc
低功耗
存储相关梳理，能否完全不用eeprom

21 AA 01 FF 83 20 
2A 62 
00 00 00 00 
8A 02 
03 00 
04 
08
3B 0C 
20 0C 
25 0C 
77 0C 
8C 0C 
3D 0C 
34 0C 
3D 0C 
00 00 00 00 
26 06 



备份域是什么？用来存储soc、容量等是否可行


需要修改休眠唤醒逻辑：目前逻辑是休眠后，短接一下就会唤醒，需要修改为开关pc13持续闭合3秒开机、关机，一个问题是不影响pa0充电立马唤醒，另一个问题是持续闭合3秒开机后，如果一直闭合不能触发关机，必须等pc13开关释放后再重新闭合3秒才能关机，休眠唤醒逻辑直接搜索IsSleepStartUp函数


需要优化、增加休眠逻辑，电池空闲、且正常时，可以进入rtc周期低功耗休眠

需求2目前不对，具体现象是大部分时间一直常亮，我调试发现经常LedBar_StartShortShow会调用，导致s_short_phase_ticks清零

IsSleepWakeupValid中调用的__delay_ms，__delay_ms前是否需要调用InitDelay
stop mode唤醒后，使用的时钟好像是hsi，你调研确认下

做一个小工具实现把你输出的文档同步到另一个文件夹，是同步复制不是剪切，我想在一个地方管理每天输出的重要文档，我可以随时设置，需要和你所有不同对话输出的文档都要同步而不仅限于当前对话


不要让我设置同步目标目录，我只是想同步所有codex app输出文档，可以让我设置同步到哪个目录


修改灯板led逻辑为：
1.电量显示：短按开关，显示电量5秒，然后熄灭
2.关机：逻辑不用改，改时间。开机状态下，干簧管持续闭合3秒后，先全亮LED,跑马灯从LED5~LED1逐一熄灭。每个LED间隔0.2秒，1秒钟跑马灯跑完。
3.开机：灯板上的干簧管达到电量显示的时间（比如0.3秒），先显示3秒的电量，然后熄灭。在此区间干簧管一直闭合（即闭合时间大于等于3秒了），
进入开机程序：跑马灯从LED1~LED5逐一亮起，再从LED5~LED1逐一熄灭，每个LED间隔0.2秒，即1秒钟跑马灯全亮，再1秒钟跑马灯全灭，然后显示电量3秒钟后，LED全灭。
注意开机时要根据soc先显示电量，IsSleepStartUp中由于要关掉看门狗，此时mcu复位过了，退出stop模式，soc应该是不知道的



修改备份域寄存器是否会影响rtc模块，一共有几个备份域寄存器，常见使用场景，在本项目中还能用在哪些地方


有问题：1、开机逻辑中,先显示3s电量，和从LED1~LED5逐一亮起，再从LED5~LED1逐一熄灭，每个LED间隔0.2秒，即1秒钟跑马灯全亮，再1秒钟跑马灯全灭，然后显示电量3秒钟后，LED全灭，这些现象都没看到 
2、长按开机一直长按会进入关机逻辑，不需要这样，只有放开后，再长按才进入休眠关键

一直长按关机问题解决了，但是开机逻辑还是有问题，1、没长按3s就进入开机了 2、开机动画不太对，动画现象：先亮了很久的led1，然后LED1~LED5逐一亮起，再从LED5~LED1逐一熄灭，而且我需要的是客户长按开机动画是：客户一按开机，立马显示电量，需要快，目前没看到电量显示，如果没按到3s，则休眠，仅仅显示一下电量，长按到3s后，再进入开机，进行开机动画显示

还是有问题，1、休眠状态下，第一次按开机键，soc显示正常，没长按3s休眠后，再次按开机查看soc，此时一直是led1亮而不是正常soc，开机后，也没看到跑马动画，还有一直长按也会触发休眠

还是运行休眠状态 充电激活，并充电吧

增加多种soc校准策略，例如ocv，静置只允许向下校准，每次变化只能是1，不允许跳变






LedBar_HandleWakePreviewBeforeBoot ????


长按gan3 3s休眠，休眠动画只需要以此熄灭led就行，目前现象是5个led依次熄灭led后，还会亮一下当前soc灯

参考E:\TODO\103 + 309 - 副本 项目，单独在当前分支开一个分支，用于把全部eeprom替换为内部flash实现参数读写


为什么还有App_E2promDeal和{UINT32 u32E2P_Pro_VolCur_WriteFlag = 0;
UINT32 u32E2P_Pro_Temp_WriteFlag = 0;
UINT32 u32E2P_Pro_Other_WriteFlag = 0;
UINT32 u32E2P_RTC_Element_WriteFlag = 0;
UINT32 u32E2P_OtherElement1_WriteFlag = 0;
UINT32 u32E2P_HeatCool_WriteFlag = 0;

UINT8 u8E2P_SocTable_WriteFlag = 0;
UINT8 u8E2P_CopperLoss_WriteFlag = 0;
UINT8 u8E2P_KB_WriteFlag = 0;
UINT8 u8E2P_KB_WritePos = 0;
}这些变量，参考工程不是已经没有了吗


stb改485收发控制,串口2 485

直接定位Sci_Upper.c，梳理当前串口收发架构，在不影响功能的前提下，能否优化当前架构，从性能、可阅读、可维护多方面，例如减少不必要的变量,还有重复的函数逻辑能否合并，例如：App_CommonUpperSCI1和App_CommonUpperSCI2，例如：gu16_CommuErrCnt_SCI1、gu8_TxEnable_SCI1、gu8_TxFinishFlag_SCI1等等这些变量，其次当前分支项目中GPIO_M_STB这个io的功能改变了，用于串口2 485的收发使能切换，高电平发送，低电平接收，注意在发送最后一个字节后，不能立即切换485为低，避免最后一个字节没发送完。注意，一切修改要注意可读性、后续可维护性，例如协议的增加、修改等等要方便修改、维护


定位App_WarnCtrl.c，在不影响功能和时序的前提下，能否优化、重构保护逻辑

评估当前的硬件i2c驱动是否合适，是否有异常恢复功能

梳理当前的休眠、唤醒以及led逻辑，能否在不影响功能的前提下优化，先给出你的想法


System_OnOFF_Func、System_Func_StartUp相关变量、逻辑是否有实际用处，能否删除


MonitorAFE逻辑是否太复杂


GPIO_M_CTR是给485电路供电，供电给高后485才能正常通信，但是功耗高，给低功耗低，PIN_INT_WK_CMNT是用于唤醒485电路，PIN_INT_WK_CMNT有上升沿中断时，代表外界要和485通信，现在需要实现逻辑：默认不给485供电省电，当PIN_INT_WK_CMNT进入中断时，开始供电正常485通讯，485正常通信窗口时间是默认30s可调，当窗口时间内没有正常485通信时，自动GPIO_M_CTR断电省电。


目前的Sci_SetTxDirection收发使能是什么逻辑，发送完成后，多久切换到接收状态，mcu发送完成，不能保证通过485电路发送完成了，因此这个切换时机很重要，哪些参数可以调整这些时间