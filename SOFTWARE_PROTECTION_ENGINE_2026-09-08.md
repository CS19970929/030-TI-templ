# 软件保护引擎重新实现

## 当前设计

本次按保护业务重新实现内部结构，替代提交 `29d27ae` 的宏展开方案。外部继续通过 `App_WarnCtrl()` 调度，通信参数、状态字、记录编号、报警回调及 MOS 控制读取方式保持。

内部只保留以下职责：

| 对象/函数 | 职责 |
|---|---|
| `protectionRules[13]` | 每类一份规则：采样、两级触发/恢复阈值、滤波参数、恢复附加时间、电流门控和时隙 |
| `protectionCounts[13][2]` | 集中保存两级独立积分计数，共 52 字节 |
| `Protection_Evaluate()` | 纯判断函数，只处理传入的样本、阈值、计数和故障状态 |
| `Protection_Process()` | 执行一个已经到期的保护级别：读状态、门控、读实时参数、判断并发布 |
| `protectionOutputs[13]` | 外部兼容映射：协议故障位、二/三级记录锁存位和枚举编号 |
| `Protection_Publish()` | 立即更新外部故障状态、记录锁存及特殊报警回调 |
| `Protection_RecordEvent()` | 唯一的环形记录写入实现 |
| `App_WarnCtrl()` | 无到期标志时直接返回；有到期项时按原顺序依次处理二级和三级 |

没有函数生成宏、26 个专用检查函数、26 个局部静态计数器、运行时函数指针或动态内存。规则用普通 C99 指定成员初始化，可直接阅读字段含义并定位到通用函数调试。

故障状态仍以现有 `g_stCellInfoReport` 为唯一存储，记录锁存仍以 `Fault_Flag_Second/Third` 为唯一存储。引擎只集中保存计数，不复制第二套活动标志。因此启动强制故障及通信清记录不会与内部状态脱节。

## 规则与维护

`trigger[LEVEL_SECOND/LEVEL_THIRD]` 表示各级触发阈值，`recovery[]` 表示各级恢复阈值；欠压、低温和 SOC 过低也使用同样的业务含义，不再要求维护者把触发/恢复换算成“大值/小值”。

- `TRIP_ABOVE`：大于等于触发阈值累积触发；小于等于恢复阈值累积恢复。
- `TRIP_BELOW`：小于等于触发阈值累积触发；大于等于恢复阈值累积恢复。
- `filterTicks`：每次检查读取当前 EEPROM 参数，两级使用各自独立计数。
- `recoveryExtraTicks`：仅充、放电过流为 3000，其余为 0。
- `gate`：普通保护始终执行；电池温度保护的新故障要求对应电流大于 1（单位 0.1 A）；已有故障始终允许恢复。
- `slotCounter/countAtLevel`：兼容原统计计数的递增位置，不参与保护判断。

| 类型 | 时隙 | 方向 | 门控 |
|---|---:|---|---|
| 单体过压、单体欠压 | 1 | 分别上越限、下越限 | 无 |
| 总压过压、总压欠压 | 2 | 分别上越限、下越限 | 无 |
| 充电过流、放电过流 | 3 | 上越限 | 无 |
| SOC 过低 | 4 | 下越限 | 无 |
| 充电过温、放电过温 | 4 | 上越限 | 对应电流 |
| MOS 过温、单体压差 | 5 | 上越限 | 无 |
| 充电低温、放电低温 | 5 | 下越限 | 对应电流 |

类型枚举的顺序保留原入口顺序：单体电压、总压、MOS、压差、放电电流、充电电流、SOC、放电温度、充电温度。它不是按时隙排序的表。每个到期类型先二级后三级。

新增保护时，在类型枚举、规则表和输出映射中添加对应项，并补充业务测试、协议映射和调度顺序测试。所有运行状态集中在计数数组，不再复制一份检查函数。现有规则的调整通常只需改规则表；不要直接改变协议位号。

## 保留的业务语义

1. 命中阈值时计数加一，未命中时减一而非清零。状态翻转后计数清零。
2. 温度电流门控关闭时保留计数，重新满足门控后续计；恢复不受电流门控限制。
3. 阈值关系无效时不改变计数、故障状态或记录。保留 16 位计数及恢复延时相加后的边界行为，没有顺带修改参数范围策略。
4. 每次有效检查都同步输出与记录锁存，不只在故障翻转时处理。清记录后，仍存在的故障在下一次有效检查重新记录。
5. 实时状态先写回，随后写记录、执行特殊回调、最后修改记录锁存。恢复时先清实时状态，再执行恢复回调并清记录锁存。
6. 只有三级压差执行原 `ERROR_VDEATLE_OVER/ERROR_REMOVE_VDEATLE_OVER` 回调。
7. 保留原检查节拍、事件先后、环形记录回绕及保留位。发布一项结果后立即对后续项可见。

时间标志由主循环 `App_SysTime()` 更新，现有检查期间没有中断写这些标志，因此每个类型检查一次到期标志后处理两级。兼容层对故障字只修改自己的掩码；当前工程对这些状态的写入位于初始化或主循环，不新增中断并发写入模型。

## 清理与兼容边界

- 删除原 26 个 `App_*Check` 内部实现，生产固件只从 `App_WarnCtrl()` 进入引擎；这些旧检查函数不在 `Fault.h` 公共接口中，也没有其他生产调用者。
- 删除无生产调用者的空 `FaultWarnRecord()`；原实际记录函数改为内部 `Protection_RecordEvent()`。
- 删除全工程无读取/写入使用的 `Fault_record_First`、`Fault_record_Second`、`FaultPoint_First`、`FaultPoint_Second`，并同步移除头文件声明。
- 保留通信正在使用的 `*_First2/Second2/Third2` 记录、三级旧记录/RTC 记录和启动诊断状态。没有为了减少变量而删除对外协议数据。
- `PRT_E2ROM_PARAS`、故障枚举、通信故障位域及实际记录锁存结构的布局均保持；`Fault.h` 仅移除四个未使用声明。
- `_hiccup_mode` 仍明确拒绝编译：原始实现引用了被注释的计数器，本来就不是可用配置。本次没有另行设计该模式。

## 验证证据

### 真实新旧源码差分

`tools/test_warn_ctrl.py` 固定读取原始提交 `9a04ed8d1aa8649422b4ba8d91407adfdf356c12` 的真实 C 实现，与当前引擎分别编译运行。旧版本只在主机测试中把原静态计数提升到可观测数组；新版本仅在主机测试中提供旧名称适配函数。生产代码没有这些适配器。

比较每拍对外故障字、记录锁存、环形记录、26 个计数器、统计计数以及回调时的状态。新引擎不再调用旧 `App_PubOPUPChk()`，因此本次比较不要求两侧具有相同的内部函数调用轨迹。

6,055,327 拍、2,564 次回调、26 个计数器的轨迹逐字节一致，共 1,260,036,200 字节。覆盖阈值边界、异常阈值、长滤波、计数/延时边界、暂停续计、恢复、清记录、记录回绕、错峰/关闭/同时到期及随机输入。报告中保留源文件哈希和轨迹哈希。

### 独立业务断言

不依赖旧代码计算预期，直接断言：上/下越限的触发与恢复周期、未命中递减、无效参数保持、16 位边界、电流 0/1/2 门控、温度计数暂停续计、无电流恢复、持续故障清记录后重新记录、过流 3003 拍恢复（测试滤波为 3）、压差回调、环形回绕和保留位不受影响。

### ARM 目标位域核对

测试从真实项目头文件提取位域，用 ARMCC 为 Cortex-M0 编译 13 个故障位和 26 个记录位的常量对象，再读取 ELF 数据逐项核对新映射。主机也执行命名位域和记录枚举断言，避免只用主机布局推断目标布局。

运行方式：

```powershell
python tools/test_warn_ctrl.py --report artifacts/protection-engine/test-report.json
```

依赖 Python 3、Git、Visual Studio 2022 C++ 和 `C:/Keil_v5/ARM/ARMCC/bin/armcc.exe`。中间 C、OBJ、EXE、完整轨迹全部位于 `LOCALAPPDATA/CodexTemp/030-TI/`。

### Keil 配对构建

工程 `CommomBQ769x0_16series_030C8T6_C.uvprojx`，目标 `Target 1`，STM32F030C8，ARMCC 5.06 update 7 build 960。使用本任务开始时的同一工程快照，对比上一版宏实现和新引擎；两次全量构建均为 0 错误、44 个既有警告，警告内容逐项一致，耗时各 4 秒。

| 字节数 | 上一版宏实现 | 当前引擎 |
|---|---:|---:|
| Code | 38656 | 34888 |
| RO-data | 2668 | 3292 |
| RW-data | 692 | 640 |
| ZI-data | 6364 | 6416 |
| Flash（Code+RO+RW） | 42016 | 38820 |
| RAM（RW+ZI） | 7056 | 7056 |

Flash 减少 3196 字节。52 字节计数从独立初始化变量收敛为零初始化数组，RW/ZI 分类变化但 RAM 总量不变。规则表位于只读区，未增加一份 RAM 配置表。

在其他任务提交 AFE 修改后，还对最新完整工程执行了集成全量构建：0 错误、44 个警告、4 秒；Code=35748、RO-data=3292、RW-data=644、ZI-data=6428，Flash=39684、RAM=7072 字节。此处总量包含其他任务的修改，不用于计算本次保护重构的增减。

正式报告、构建日志及固件位于 `artifacts/protection-engine/`。交付固件采用上述最新集成构建；构建输入哈希及固件大小/哈希见 `build-report.json`。它与 `artifacts/warn-refactor/` 的历史宏版本证据分开保存。

## 验证边界

这次验证支持当前配置下的业务规则、通信状态、记录和报警兼容性；没有烧录目标板，也未测量真实执行耗时和电气动作时刻。不能把逐拍回归或代码体积降低表述为精确微秒时序证明。没有修改 MCU 寄存器、底层驱动或硬件保护配置。
