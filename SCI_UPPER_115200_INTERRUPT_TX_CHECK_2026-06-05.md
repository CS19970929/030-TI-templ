# 上位机 115200 通讯异常软件排查记录

## 背景

- 简易上位机在 19200 下稳定。
- 同一套读取逻辑在 115200 下出现大量 CRC 错误和少量超时。
- 旧固件发送逻辑为主循环阻塞等待 TXE，改为 TXE/TC 中断发送后问题明显。

## 软件侧判断

1. 协议组帧路径未发现直接改坏 CRC 的主路径：
   - 应答仍在 `Sci_ACK_0x03()` / `Sci_ACK_0x06_0x10()` 中一次性组帧并计算 CRC。
   - `TX_BUSY` 期间主循环不重新组帧。
   - `TC` 后才置 `TX_COMPLETE` 并恢复接收。

2. 更可疑的软件差异是发送时序：
   - 当前工程默认 `_HSE_NOPLL_8M`，系统时钟为 8MHz。
   - 115200 下一个 8N1 字符约 86.8us。
   - 中断发送必须在每个 TXE 周期内及时装入下一个字节；ISR 开销、同级中断或异常路径延迟都可能造成帧内空隙。
   - 旧阻塞发送在主循环中等待 TXE 后立即写 TDR，帧内软件空隙更少。

3. 额外风险：
   - USART1/USART2 GPIO 原配置为 `GPIO_Speed_2MHz`，对 115200 通常可用，但边沿裕量偏保守。
   - `fputc()` 默认走 USART1；如果后续打开 `printf` 并且上位机接 USART1，会把调试字符插入协议流。

## 本次修改

- `Sci_StartTx()`：
  - 发送开始时关闭 RX/RXNEIE/TXEIE/TCIE。
  - 清 TC。
  - 若 TXE 已空，立即预装第 1 字节，减少一次中断等待。
  - 后续字节继续由 TXE 中断装载，最后一字节装载后切到 TC 中断。

- `Sci_TxISR_Deal()`：
  - 去掉发送热路径中的 `USART_GetITStatus()` / `USART_GetFlagStatus()` 调用。
  - 直接使用 `ISR/CR1/ICR` 判断和清除 TXE/TC，降低 8MHz 下的 ISR 开销。
  - 仍保持最后一字节 TC 后才置发送完成。

- `USART1_IRQHandler()` / `USART2_IRQHandler()`：
  - 使用一次 `ISR/CR1` 快照判断中断来源。
  - TXE/TC 优先处理，降低发送期间延迟。
  - 错误检查只在错误标志出现时调用。

- SCI1/SCI2 GPIO：
  - 串口 AF 引脚速度从 `GPIO_Speed_2MHz` 调整为 `GPIO_Speed_50MHz`，提升 115200 边沿裕量。

## 验证清单

- SCI2 使用 115200，简易上位机连续读取电池信息：
  - `CRC` 计数应明显下降或归零。
  - `Timeout` 不应持续增加。
  - `RX OK` 应接近 `TX` 次数。

- SCI2 使用逻辑分析仪观察：
  - 单帧内字节连续性。
  - 帧内是否存在明显大于一个字符时间的空隙。
  - 最后一字节完成后才恢复接收。

- SCI1/SCI2 兼容性：
  - Modbus `0x03/0x06/0x10` 响应格式不变。
  - P12 `0x80/0x82` 响应格式不变。

## 结论

本次补丁针对“中断发送在 8MHz + 115200 下响应裕量不足”这一软件可能性做最小优化。如果 115200 仍存在大量 CRC 错误，应进一步用逻辑分析仪确认 TXD 波形、RS485/USB 转换器方向控制和上位机连续帧解析能力。
