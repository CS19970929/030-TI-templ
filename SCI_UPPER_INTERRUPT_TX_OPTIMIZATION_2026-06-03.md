# 串口接收超时与中断发送优化说明

## 变更目标

本次优化覆盖 `SCI1` 和 `SCI2` 的上位机通信收发流程，保持现有 Modbus RTU 与 P12 私有协议帧格式不变。主要目标是：

- 接收半包超过 `30ms` 后自动丢弃并恢复接收。
- 发送从主循环轮询 `TXE` 改为 USART `TXE + TC` 中断发送。
- 最后一字节等待 `TC` 完成后才恢复接收，避免过早切回接收状态。

## 收发状态机

状态定义调整为：

- `RS485_STA_IDLE`：空闲或正在接收半包。
- `RS485_STA_RX_COMPLETE`：ISR 已收到完整帧，等待主循环解析。
- `RS485_STA_RX_OK`：主循环完成协议处理，等待构造应答。
- `RS485_STA_TX_BUSY`：应答已准备，正在由 `TXE/TC` 中断发送。
- `RS485_STA_TX_COMPLETE`：`TC` 已确认最后一字节发送完成，等待主循环复位帧状态并恢复接收。

## 接收超时策略

- `SCI1` 和 `SCI2` 均沿用系统 `10ms` tick。
- 当状态为 `IDLE` 且 `ptr_no > 0` 时，连续 3 个 tick 未收到新字节即认为半包超时。
- 超时后仅复位当前帧状态，不回复错误帧。
- 每收到有效字节会清零对应 `gu8_RxTimeoutTick_SCIx`。
- `0x10` 写多个寄存器帧会校验 `byte count == register count * 2`，并确认总长度不超过 `RS485_MAX_BUFFER_SIZE`。

## 中断发送流程

1. 主循环生成应答后调用发送启动逻辑。
2. 发送启动时关闭接收和 `RXNEIE`，清 `TC`，开启 `TXEIE`。
3. `TXE` 中断逐字节写入 `TDR`。
4. 最后一字节写入后关闭 `TXEIE`，清 `TC` 并开启 `TCIE`。
5. `TC` 中断确认移位寄存器已发送完成后关闭发送中断，置 `RS485_STA_TX_COMPLETE` 和发送完成标志。
6. 主循环看到发送完成后复位帧状态，重新开启接收和 `RXNEIE`。

## 兼容性

- `SCI1` 保持 19200、Modbus RTU。
- `SCI2` 保持 115200，同时兼容 Modbus RTU 和 P12 帧头 `0x21 0xAA`。
- P12 静态信息 `0x80/0x81` 和动态信息 `0x82/0x83` 的响应内容不变。
- 参数写入、保护参数、SOC、Flash 升级触发等业务逻辑未做扩大重构。

## 手工验证清单

- SCI1 发送合法 `0x03/0x06/0x10` 帧，确认应答完整且发送后可继续接收。
- SCI1 发送半帧后等待超过 `30ms`，再发送合法帧，确认不被前一半包污染。
- SCI2 分别验证 Modbus 帧和 P12 `0x80/0x82` 请求。
- 构造非法 `0x10 byte count`、超长帧、错误帧头，确认不会越界写缓冲。
- 用串口工具或逻辑分析仪确认最后一字节发完后才恢复接收。
