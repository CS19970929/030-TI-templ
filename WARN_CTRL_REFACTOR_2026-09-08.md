# 保护逻辑统一重构与等价验证

> 历史阶段说明：本文记录提交 `29d27ae` 的宏实现。当前已改为普通 C 保护引擎，见 [软件保护引擎重新实现](SOFTWARE_PROTECTION_ENGINE_2026-09-08.md)。历史报告对应当时源码；当前测试脚本已适配新架构。

## 变更结果

`Code/Source/Fault.c` 从 1403 行缩减到 232 行，减少 1171 行（约 83.5%）。26 个保护检查函数统一由一份流程模板和一份配置清单生成，保留原函数名称、独立静态计数器以及直接位域访问。

主要实现：

- `WARN_RULES`：唯一的保护配置清单，也决定 `App_WarnCtrl()` 的原有调用顺序。
- `WARN_DEFINE`：唯一的参数读取、滤波、故障状态写回、记录锁存及回调流程。
- `WARN_GATE_*`：普通保护、充电温度保护、放电温度保护的检查门控。
- `WARN_RAISE_*` / `WARN_CLEAR_*`：仅三级压差保护执行原系统错误回调。
- `App_PubOPUPChk()`、记录枚举、保护参数布局、故障位域、通信接口和主循环位置保持原样。

这是编译期展开。没有增加运行时配置表、函数指针遍历、动态内存、按枚举推算位掩码或集中提交故障状态。可继续通过原 `App_*Check` 名称设置调试断点。

## 配置与维护

每项配置占三行，参数分组如下：

1. 原函数名、时隙编号、故障级别、实时故障位、记录枚举。
2. 采样字段、上阈值参数、下阈值参数。
3. 滤波参数、下阈值额外延时、逻辑方向、电流门控、统计动作、回调策略。

例如三级单体欠压的上阈值为恢复电压，下阈值为保护电压，逻辑方向为 0。三级充电过流的上阈值为保护电流，下阈值为恢复电流，逻辑方向为 1，下阈值延时额外增加 `CurOverFaultDelay`。不要把“上阈值/下阈值”一律理解为“触发/恢复”。

| 保护类型 | 时隙 | 采样字段 | 门控 | 特殊处理 |
|---|---:|---|---|---|
| 单体过压 | 1 | `u16VCellMax` | 始终检查 | 二级恢复用 First，三级恢复用 Rcv |
| 单体欠压 | 1 | `u16VCellMin` | 始终检查 | 三级函数递增 `cnt_10ms1` |
| 总压过压 | 2 | `u16VCellTotle` | 始终检查 | 保留原阈值单位和来源 |
| 总压欠压 | 2 | `u16VCellTotle` | 始终检查 | 三级函数递增 `cnt_10ms2` |
| MOS 过温 | 5 | `u16Temperature[MOS_TEMP1]` | 始终检查 | 不套用电池温度电流门控 |
| 单体压差 | 5 | `u16VCellDelta` | 始终检查 | 二级递增 `cnt_10ms5`；三级执行故障/恢复回调 |
| 放电过流 | 3 | `u16IDischg` | 始终检查 | 恢复延时加 3000；三级递增 `cnt_10ms3` |
| 充电过流 | 3 | `u16Ichg` | 始终检查 | 恢复延时加 3000 |
| SOC 过低 | 4 | `SocElement.u16Soc` | 始终检查 | 三级函数递增 `cnt_10ms4` |
| 放电过温 | 4 | `u16TempMax` | 放电电流 | 已触发故障的恢复不受电流限制 |
| 放电低温 | 5 | `u16TempMin` | 放电电流 | 同上 |
| 充电过温 | 4 | `u16TempMax` | 充电电流 | 同上 |
| 充电低温 | 5 | `u16TempMin` | 充电电流 | 同上 |

每种保护仍按二级、三级顺序调用；上述表格顺序就是原入口的调用顺序。增加保护时，必须显式填写状态位和记录枚举：二级记录位域的温度位顺序与枚举不完全一致。

## 保持的行为约束

### 调度与采样

五个时隙按原设计错开 2 ms，各项正常每 10 ms 检查一次。仍由每个检查函数读取自己的时间标志；不清除标志，不增加启动屏蔽，不补偿主循环错过的节拍，不重排检查，也不改变统计计数的递增位置。采样及参数在每次实际检查时读取，保留在线参数修改行为。

### 滤波和恢复

- 比较保留 `>=`、`<=`，触发计数先加一再比较。
- 条件不满足时计数减一，而非清零。
- 故障状态翻转时清零该项计数；每项二级和三级计数独立。
- 参数无效时不写回故障、不更新记录，计数保持。
- `UINT16` 计数及过流恢复延时相加后转换的原有边界行为保持，包括溢出边界；本次不夹带参数范围修正。
- 无温度故障且对应电流不严格大于 1 时，跳过滤波并保留计数；已有温度故障时仍检查恢复。门限的单位为 0.1 A。

### 状态、记录与回调

公共检查返回有效后，先写实时故障状态；若故障存在且记录锁存为 0，依次调用两个记录接口、执行特殊回调、再置记录锁存。恢复时先执行恢复回调，再清记录锁存。

通信清记录仅清记录及锁存，不清实时保护状态。原逻辑会在下一次有效检查中重新记录仍存在的故障；重构保留该行为，不能改成只在实时状态翻转时记录。

`FaultWarnRecord()` 原函数体由 `#if 0` 禁用，现保留空接口并删除死代码；实际环形记录仍由原 `FaultWarnRecord2()` 完成。原 `_hiccup_mode` 引用了已注释的局部计数器，启用时本就无法编译；现使用明确的编译错误保留这一未支持状态，没有悄悄启用或替换恢复策略。

## 验证

### 主机逐拍等价测试

脚本：`tools/test_warn_ctrl.py`。

```powershell
python tools/test_warn_ctrl.py --report artifacts/warn-refactor/test-report.json
```

基准固定读取 Git 提交 `9a04ed8d1aa8649422b4ba8d91407adfdf356c12` 中的真实旧 `Fault.c`，与当前文件分别通过 MSVC `/O2` 编译。两侧使用同一份真实公共滤波函数和从项目头文件提取的保护参数、故障位域、温度索引及状态结构。硬件及系统错误回调用主机桩替代。

测试覆盖 26 项保护的上下阈值及相邻值、无效/相等阈值、0/1/3/50000/62535/62536/65535 滤波参数、16 位计数边界、过流额外恢复延时、温度电流 0/1/2 门控、暂停后续计、故障恢复、记录清除及环形回绕、时隙关闭与错峰/同时置位、在线参数变化、外部状态变化及确定性随机扰动。

结果：6,055,327 拍、26 个独立计数器、2,564 次特殊回调，两侧 1,431,322,380 字节轨迹逐字节相等。比较包括每次滤波调用的输入输出及计数、每拍全部已登记计数器、保护标志、记录锁存、环形记录、统计计数和回调时刻的状态。报告附源文件 SHA-256 和轨迹 SHA-256；比较本身不是只比较摘要。

### Keil 全量构建

工程：`CommomBQ769x0_16series_030C8T6_C.uvprojx`；目标：`Target 1`；芯片：STM32F030C8；编译器：ARMCC 5.06 update 7 build 960。

最终基准与重构版使用相同的工作区快照，唯一差异为 `Fault.c`，均执行全量重编译。快照包含同时完成的串口修复，因此总固件大小以此最终配对构建为准。

| 项目 | 基准 | 重构后 | 差值 |
|---|---:|---:|---:|
| Code | 38724 | 38656 | -68 |
| RO-data | 2668 | 2668 | 0 |
| RW-data | 692 | 692 | 0 |
| ZI-data | 6364 | 6364 | 0 |
| Flash（Code+RO+RW） | 42084 | 42016 | -68 |
| RAM（RW+ZI） | 7056 | 7056 | 0 |
| 错误 | 0 | 0 | 0 |
| 警告 | 44 | 44 | 内容逐项一致 |
| 构建耗时 | 4 秒 | 4 秒 | 0 |

反汇编检查：8 个温度检查函数各减少 3～4 条静态指令，其他 18 个检查函数及入口的静态指令数量不变。静态指令数量不是最坏执行时间；不据此声称指令周期或绝对动作时刻完全相同。

构建技能的本机共享 `tool_config` 模块缺失，本次通过进程内最小兼容模块及显式 `UV4.exe` 路径运行原 `keil_builder.py`；未修改技能安装或用户工具配置。

## 交付物与验证边界

- 实现：`Code/Source/Fault.c`。
- 可复现逻辑测试：`tools/test_warn_ctrl.py`。
- 正式证据：`artifacts/warn-refactor/test-report.json`、`build-report.json`、两份构建日志、`instruction-comparison.json`。
- 固件：`artifacts/warn-refactor/firmware/` 中的 AXF、HEX、BIN；文件大小、SHA-256 和构建源文件清单见 `build-report.json`。

所有编译快照、OBJ、中间脚本和完整测试轨迹均位于 `LOCALAPPDATA/CodexTemp/030-TI/`，不在项目中创建临时目录。项目内只保留正式报告和构建产物。

此次完成源码语义检查、主机差分验证和目标编译，未烧录设备或测量板上保护动作。测试是对所列场景的强回归证据，不是对任意输入及中断交错的形式化证明。滤波节拍、状态机和事件顺序保持；精确执行耗时仍需硬件测量。此次不涉及 MCU 寄存器或底层驱动修改。
