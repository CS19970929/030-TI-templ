# Modbus 请求边界与 HardFault 修复

## 问题与结果

ST-Link 捕获 USART2 请求 `01 03 00 00 00 03 05 CB` 触发 HardFault。CRC 合法，但地址 0x0000 未被应答函数支持；正常应答路径没有给局部长度赋值，CRC 循环一直读到 SRAM 外的 0x20002000。详细现场见 STLINK_RS485_HARDFAULT_2026-09-08.md。

现在原请求生成 `01 83 02 C0 F1`，表示非法地址，后续合法请求仍可处理。修复覆盖共用协议业务，USART1 和 USART2 均受保护，不改变 PB14 仅激活运行中 485 的约定。

## 校验边界

1. CRC 计算前验证帧长、从机地址和功能码；0x03/0x06 必须恰好 8 字节，0x10 的寄存器数、数据字节数、实际帧长必须相符且落在接收缓冲区容量内。CRC 错误或帧格式无效直接清理并重新接收，不执行写操作，也不续期 485 电源窗口。
2. 0x03 保持 16 位寄存器数量，先检查数量和完整读取范围，再转换为字节长度。接收/应答缓冲区为 251 字节，一帧最多读 123 个寄存器，但仍受实际数据页长度限制。数量零或超上限返回 0x03；未映射地址、跨页及越过页尾返回 0x02。采用差值检查，避免地址加法回绕。
3. 应答构造再次校验请求；只对已验证的数据页进行序列化与拷贝。所有路径都有确定的长度，CRC 前的长度受响应容量约束。编译期检查确保完整页序列化不会超出共享缓冲区，也不会超过底层结构的大小。
4. 不支持的 0x06/0x10 地址返回 0x02，已支持的固定长度写操作数量不符返回 0x03；原来为空的 SOC 表、铜损、RTC 写处理不再虚报成功。未知功能码仍按原接收策略丢弃。
5. 补齐可从写请求进入的危险边界：校准系数数组索引、保护参数块末端、产品标识最大 32 字节、串数索引和分流电阻/并联数量非零。系统参数在全部校验通过后才更新，拒绝时不修改参数或 EEPROM 写标志。
6. 修复旧 SOC_TABLE_LIFEPO2 读分支未给临时值赋值的问题，回退到现有 SOC_Table_Set。移动 EVENT_RECORD_LENGTH 到 LogRecord.h，让校验与事件页生成共享同一长度定义。

广播处理和其他已有业务指令的语义保持现状，本次没有把整个私有寄存器协议重写为另一套映射。对数据页之外的读取不再提供旧缓冲区残留内容。

## 读取映射

| 起始地址 | 范围/数量 | 说明 |
| --- | --- | --- |
| 0x2000 | 2 × KB_NUM 个字，当前 94 | 校准 K/B |
| 0x2100 | 65 个字 | 保护参数 |
| 0x2200 | 86 个字 | SOC 表、铜损和 RTC 保留数据 |
| 0x2300 | 56 个字 | 其他参数和加热参数 |
| 0xD000 | 63 个字 | 状态数据 |
| 0xD100 | 33 个字 | 故障和系统状态 |
| 0xD200 | 1 个字 | 保留数据 |
| 0xC000 | 最多 5 个字 | LCD 页 |
| 0xC001 | 最多 70 个字 | 故障记录页 |
| 0xC002 | 最多 48 个字 | 三组产品标识 |
| 0xC008 | 最多 100 个字 | 事件记录页 |

C000/C001/C002/C008 是原协议的整页选择地址，保留从页开头取数的行为。其他数据区支持范围内偏移读取；不允许跨页或用地址空洞读取相邻数据。

## 自动验证

- `python tools/test_sci_protocol.py`：使用真实 C CRC、读请求解析、全部读页序列化、应答构造和协议分发代码，在 MSVC /RTC1 与缓冲区哨兵下通过。覆盖原死机报文的精确异常响应、全部 65536 个地址 × 8 组数量（共 524288 组合）、页尾、数量溢出、CRC 错误、截断/超长帧、0x10 长度一致性、非法请求后恢复、有效数据偏移、无效系统参数/产品标识写入。外设操作及协议分发测试中的写业务采用桩；系统参数和产品标识保护另调用实际叶函数验证。
- `python tools/test_sci_transport.py`：原有传输和电源窗口回归通过，覆盖双串口隔离、接收边界、TXE/TC 方向、发送看门狗以及默认 30 秒空闲断电。
- AddressSanitizer 运行库在本机无法启动（0xC0000135），因此最终测试使用 /RTC1 与哨兵，并未宣称通过 ASan。这些主机测试不替代硬件回归或对整个固件的形式化证明。
- Keil 工程 CommomBQ769x0_16series_030C8T6_C.uvprojx，Target 1，STM32F030C8 / ARMCC 5.06 update 7：编译通过。首次相关构建 0 错误、30 警告、5 秒；最终增量编译 0 错误、7 警告、1 秒。最终警告均为既有 P12_GetVersionWord 未使用局部变量；未清理项目其他警告。
- 最终 Code=36496，RO-data=3292，RW-data=656，ZI-data=6432；Flash 合计 40444 字节，RAM 合计 7088 字节。
- Objects 下同工程名产物：AXF 819084 字节、HEX 112212 字节、BIN 39876 字节。原始二进制与链接器 Flash 占用统计口径不同。

构建沿用 build-keil 技能流程；其脚本缺少 tool_config，使用本机 UV4 等价构建。日志位于 LOCALAPPDATA/CodexTemp/030-TI/modbus-bounds-fix-20260908，测试中间文件位于同用户临时区的 sci-protocol-tests 和 sci-transport-tests。

## 硬件状态及复测

已按用户要求退出 GDB/OpenOCD，释放 ST-Link；没有重新连接、复位或烧录修复固件。板上仍为旧程序，自动测试结果不能视为新固件上板验证。

烧录新固件后，应对两路串口分别发送原死机报文、有效读请求、超大/零数量、跨页和错误 CRC 请求，再确认后续正常通信。非法地址应收到异常 0x02，非法读数量应收到异常 0x03，CRC 错误帧无应答。保留现有 485 激活和空闲断电时序。
