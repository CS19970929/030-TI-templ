# 五项客户关机问题的最小修复

基线：`0db3d0dfde0208128f4c4952683d3a728db81c2b`，分支 `d012-bug`。
没有合入此前重构或后续全面审核修复；SleepDeal、启动长按唤醒、Flash/参数和充电显示源码保持基线。

## 仅处理这些问题

1. 放电中把手离位连续 2 秒：关闭输出、灭灯、请求深度休眠；2 秒内归位取消计时。
2. 放电中进水：报警锁存，120 秒后灭灯、关闭输出、请求深度休眠。
3. 充电中进水后提把手：立即执行同一关机路径。
4. 进水后开机：启动设置的进水报警参与同一 120 秒超时。
5. 进水后查看电量：同上，不再依赖检测器断电后的实时电平。

## 改动及原因

- main 独立调用 `ganhuangguan_Logi()`，函数用现有 10 ms tick 去重，不再被 AFE 的 200 ms 采样、参数写入门控阻断。没有将业务或 I2C 操作搬到中断。
- 仅把把手 2 秒、进水 120 秒改为 16 位无符号 tick 差值；主循环短暂延迟不会把计时按调用次数拉长。已有 10 ms tick 约 655 秒回绕，测试覆盖跨回绕。
- 复用 LED 已有进水报警锁存状态；进水检测断电后即便读回无水，计时和提把手关机仍继续。
- 关机统一强制灭灯；报警期间上盖关机动画不能抢占报警。后续显示请求不能重新点灯。
- 关闭 MOS 不依赖下一次 AFE 采样；按 200 ms 节流重试关闭，最终驱动输出禁止覆盖报警/关机的关闭请求。

“立即”指主循环检测后的灭灯和关断请求，不代表实测已达到 STOP 静态电流。120 秒从启动完成后业务首次处理锁存报警开始；保留原启动流程。

## 验证

`python tools/test_minimal_five_shutdown.py`：11/11 通过。
使用生产 GAN、LED 和最终 MOS 输出函数，替代 GPIO/I2C/休眠入口；覆盖五项投诉、2 秒内归位、输入恢复、同 tick 重复调用、延迟调度、回绕、报警与动画竞争、充电把手操作。
启动进水两场景覆盖公共 `WATER_ALARM` 启动分支，不等价于完整 MCU 复位/硬件唤醒测试。

Keil `Target 1`，STM32F030C8，ARMCC 5.06 update 7，全量编译 6 秒：0 errors / 46 warnings。
Code=37156、RO-data=3292、RW-data=672、ZI-data=6432；Flash=41120、RAM=7104 字节。
产物、大小及 SHA256 位于 `artifacts/minimal-five-20260921/manifest.json`，日志同目录。

本次构建保留用户已有未提交测试配置：默认 8 串、watchdog 关闭、MOS 温度固定测试值、EEPROM 标志 `0x1909`。
这些配置没有混入修复提交，构建产物与纯提交默认配置不同。
未烧录、未板测；仍需在客户硬件逐项确认五项需求及实际休眠电流。基线中的其他问题不在本次修复范围。
