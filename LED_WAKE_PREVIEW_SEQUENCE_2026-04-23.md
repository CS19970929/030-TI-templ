# 灯板唤醒预览与开关机时序调整

## 变更目的

本次修改针对灯板 `LED1~LED5` 的显示时序，满足以下需求：

1. 开机状态短按干簧管，只显示当前电量 `5s`，然后全部熄灭。
2. 开机状态长按干簧管 `3s` 关机，关机动画改为：
   先全亮，再按 `LED5 -> LED1` 每 `0.2s` 逐个熄灭，`1s` 跑完。
3. 关机状态下，干簧管闭合达到电量显示阈值后：
   立即显示电量，优先保证客户按下后快速看到电量。
4. 如果按住累计达到 `3s`，继续进入开机流程：
   `LED1 -> LED5` 每 `0.2s` 逐个点亮，`1s` 全亮；
   再按 `LED5 -> LED1` 每 `0.2s` 逐个熄灭，`1s` 全灭；
   最后再显示电量 `3s`，然后全部熄灭。
5. 如果未按满 `3s` 就松手，则仅显示电量预览，不进入开机动画，随后回到休眠。

## 关键实现

### 1. 开机态短按显示

- `LedBar.c`
- 常态下 LED 默认熄灭。
- 仅在开机状态、短按释放后进入 `LED_UI_SHORT_SHOW`。
- 按实时 `SOC` 显示 `5s`，超时后熄灭。

### 2. 关机态先预览电量，再决定是否开机

- `SleepDeal.c`
- 睡眠态下 `DI1/PC13` 闭合约 `50ms` 就视为有效唤醒，优先提升“按下即显示电量”的响应速度。
- 到达该阈值时写入“电量预览唤醒”标志。

- `LedBar.c`
- 启动时如果检测到“电量预览唤醒”标志，则先进入 `LED_UI_WAKE_PREVIEW_SHOW`。
- 预览阶段立即显示缓存 `SOC`，并在按住期间累计长按时间。
- 当累计长按达到 `3s` 时，立刻进入开机跑马灯。
- 若未按满 `3s` 就松手，则不进入开机流程，直接重新进入睡眠。

### 3. 开机态长按关机动画

- `LedBar.c`
- 仅在已经开机且 UI 处于 `LED_UI_NORMAL` 时，长按 `3s` 触发关机动画。
- 动画顺序：
  - 初始全亮；
  - 再按 `LED5 -> LED1` 每 `0.2s` 熄灭一个；
  - `1s` 完成后请求进入睡眠。

### 4. 开机动画与开机后再显示电量

- `LedBar.c`
- 预览阶段持续闭合时，进入两段式开机动画：
  - `LED_UI_BOOT_ANIM_ON`：`LED1 -> LED5` 每 `0.2s` 点亮；
  - `LED_UI_BOOT_ANIM_OFF`：`LED5 -> LED1` 每 `0.2s` 熄灭。
- 动画结束后进入 `LED_UI_BOOT_POST_SHOW`，再显示缓存 `SOC` `3s`，随后熄灭。

### 5. 休眠唤醒后 SOC 不可靠的问题

用户特别指出：`IsSleepStartUp()` 场景下 MCU 已经过复位并退出 `STOP`，此时不能依赖实时 `SOC` 重新计算结果。

为解决这个问题，本次改动增加了休眠前 `SOC` 缓存机制：

- `SleepDeal.c`
  - 进入休眠流程前，把当前 `g_stCellInfoReport.SocElement.u16Soc` 写入备份寄存器。
- `Flash.c/.h`
  - 新增唤醒显示状态接口。
  - 使用 `RTC->BKP3R/BKP4R` 保存：
    - 唤醒显示模式
    - 休眠前缓存 `SOC`
  - 通过正反码校验保证数据有效性。
- `LedBar.c`
  - 预览显示、开机动画后的 `3s` 电量显示，均优先使用缓存 `SOC`。

## 影响文件

- `Code/Source/Flash.c`
- `Code/Source/Flash.h`
- `Code/Source/LedBar.c`
- `Code/Source/LedBar.h`
- `Code/Source/SleepDeal.c`
- `Code/Source/main.c`

## 验证结果

- 2026-04-23 使用 `Keil 5 / UV4.exe` 对工程 `CommomBQ769x0_16series_030C8T6_C.uvprojx`
  的 `Target 1` 进行了重新编译。
- 结果：`0 Error(s), 47 Warning(s)`。
- 产物已更新：
  - `Objects/CommomBQ769x0_16series_030C8T6_C.axf`
  - `Objects/CommomBQ769x0_16series_030C8T6_C.hex`
  - `Objects/CommomBQ769x0_16series_030C8T6_C.bin`

## 二次修正

- 2026-04-23 二次修正了两个实际联调问题：
  - 休眠唤醒后的预览状态在后续初始化阶段可能被 `RTC` 备份域处理影响，因此在 `IsSleepStartUp()` 唤醒返回后，先把唤醒显示状态抓到 RAM 阴影变量，再由 `LedBar_StartUp()` 读取。
  - 长按开机成功后，如果用户一直不松手，原逻辑会把同一次长按继续累计成关机长按。现已改成进入开机序列后必须先松手，后续再次长按才允许进入关机/休眠逻辑。
  - 开机预览阶段改成“按下后立即显示电量，按住累计到 `3s` 才开机”。如果不到 `3s` 就松手，仅做电量预览，不进入开机动画。

## 三次修正

- 2026-04-23 再次调整为“预览前置”方案：
  - `DI1` 唤醒后，不再等系统完整启动后才由 `APP_LedBar()` 做预览判定。
  - 改为在 `IsSleepStartUp()` 的休眠唤醒返回后、`MCU_RESET()` 之前，先直接点亮灯板显示缓存电量。
  - 在这一阶段持续检测按键：
    - 达到 `3s`：写入 `WAKE_DISPLAY_MODE_BOOT_SEQUENCE`，随后复位进入正式开机，并在 `LedBar_StartUp()` 中执行开机动画。
    - 未到 `3s` 松手：直接熄灯并重新回到休眠等待，不进入正式开机。
- 这一版的目的就是彻底避免“短按已经进入系统、但灯效还没来得及接管”的问题。

## 四次修正

- 2026-04-23 根据板上联调现象，再次把“开机跑马灯”从 `LedBar_StartUp()` 后移到了 `APP_LedBar()`：
  - `LedBar_StartUp()` 仅保留 `BOOT_SEQUENCE` 挂起标记，不再在初始化阶段直接启动跑马灯。
  - 只有在 `SystemStatus.bits.b1StartUpBMS == 0`、`APP_LedBar()` 真正开始进入主循环显示逻辑后，才启动 `LED1 -> LED5` 点亮、`LED5 -> LED1` 熄灭的开机动画。
  - 这样可以把“按下立刻显示电量”和“正式开机后的跑马灯”彻底分开，避免开机过程中被系统启动时序打断。
- 同时去掉了 LED 动画对系统开机功能位的依赖：
  - `LedBar_IsWakePreviewPending()` 对外固定返回 `0`，不再让 `InitVar()` 借此改动 `System_OnOFF_Func`。
  - LED 只负责自己的预览和动画，不再介入 BMS 其它开机功能状态。
- 开机跑马灯的首帧也做了顺滑处理：
  - 进入开机动画时立即点亮 `LED1`，后续再按 `0.2s` 间隔补齐 `LED2 -> LED5`，避免动画开始前出现一段空白或停顿感。

## 五次修正

- 2026-04-23 针对再次联调暴露出的 3 个问题，补了两处关键修正：
  - 第一次短按预览结束回休眠后，第二次再按只亮 `LED1`：
    - 根因是预览退出时把备份域里的显示状态连同缓存 `SOC` 一起清成了 `0`。
    - 现改为在预览退出回休眠前，重新把本次读取到的缓存 `SOC` 写回备份域，只清掉显示模式，不再丢失电量值。
  - 长按开机后没有明显跑马灯，且同一次一直长按还会再次掉进休眠：
    - 开机序列改成 `BOOT_DELAY -> BOOT_ANIM_ON -> BOOT_ANIM_OFF -> BOOT_POST_SHOW`。
    - `BOOT_DELAY` 在 `APP_LedBar()` 内部延时 `1s` 后再启动跑马灯，避免刚复位完成时动画又被其它初始化过程打断。
    - 同时这整个阶段持续保持“必须先松手”的门控，不再由 `LedBar` 主动改动 `System_OnOFF_Func`，避免同一次长按又被判成关机长按。
